import Bookshelf from "../bookshelf/Bookshelf.jsx";

/*
export class ApiHelper {

    async fetchBookshelf(): Promise<Bookshelf[]> {

        const fetchData = (ressource) => {
            const options = makeOptions("GET", true); //True add's the token
            return fetch(URL + ressource, options).then(handleHttpErrors);
        }


        return fetch('http://localhost:8080/hobbies/{user_name}')
            .then(res => res.json())
            .then(hobbyData => {
                return hobbyData;
            });
    }

}

 */